import { Profile } from '../Profile';
import { HeaderContainer, CompanyIcon } from './Header.styles';

export const Header = () => (
  <HeaderContainer>
    <CompanyIcon />
    <Profile />
  </HeaderContainer>
);
